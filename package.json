{
  "name": "journal-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "core",
    "mobile"
  ],
  "scripts": {
    "test": "npm run test --workspace @journal/core",
    "typecheck": "npm run typecheck --workspace @journal/core && npm run typecheck --workspace mobile",
    "lint:deps": "depcruise core"
  },
  "devDependencies": {
    "dependency-cruiser": "^17.4.3"
  }
}
